// import { execSync } from 'child_process'

// import { PRISMA_SCHEMA_PAT} from '@/src/shared/config/prisma-path'

// try {
//   console.log('🧹 Resetting database...')
//   execSync(`npx prisma migrate reset --schema=${PRISMA_SCHEMA_PATH} --force --skip-seed`, {
//     stdio: 'inherit'
//   })
//   console.log('✅ Reset completed.')
// } catch (err) {
//   console.error('❌ Reset failed', err)
//   process.exit(1)
// }
