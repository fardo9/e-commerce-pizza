import path from 'path'

export const prismaSchemaPath = path.resolve(
  __dirname,
  '../../../src/server/infrastructure/db/prisma/schema.prisma'
)

export const prismaSeedPath = path.resolve(
  __dirname,
  '../../../src/server/infrastructure/db/prisma/schema.prisma'
)
